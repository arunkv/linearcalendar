/**
 * Active locale for Linear Calendar.
 *
 * To switch language, change the import below to the desired locale file,
 * e.g. `import t from './fr.js'` for French.
 *
 * Every locale file must export a default object with the same shape as en.js.
 */
import t from './en.js'
export default t
